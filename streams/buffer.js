//Buffer => É uma representação de um espaço na memória do computador, usado especificamente para transitar dados de uma maneira muito rápida

const buffer = Buffer.from("Hello World");

console.log(buffer);
console.log(buffer.toString());
